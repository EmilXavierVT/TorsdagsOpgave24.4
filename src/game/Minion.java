package game;

public class Minion extends Creature {
    public Minion(String description, String name, int healthPoints, int damagePoints, int successRate) {
        super(description, name, healthPoints, damagePoints, successRate);
    }
}
